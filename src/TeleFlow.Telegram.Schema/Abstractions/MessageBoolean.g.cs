// <auto-generated>
//   This file was generated by TeleFlow.Telegram.SchemaGenerator.
//   Kind: Abstraction
//   Source: https://core.telegram.org/bots/api
//   Telegram Bot API version: 10.1
//   Telegram Bot API release: 2026-06-11
//   Telegram Bot API changelog: https://core.telegram.org/bots/api-changelog#june-11-2026
//   Do not edit this file manually.
// </auto-generated>

#nullable enable
using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;
using TeleFlow.Telegram.Schema.Types;

namespace TeleFlow.Telegram.Schema.Abstractions;

/// <summary>
/// Represents the Telegram Bot API type expression &apos;Message or Boolean&apos;.
/// </summary>
[JsonConverter(typeof(MessageBooleanJsonConverter))]
public sealed partial record class MessageBoolean
{
    private MessageBoolean(Message value)
    {
        ArgumentNullException.ThrowIfNull(value);
        Message = value;
    }

    private MessageBoolean(bool value)
    {
        Boolean = value;
    }

    /// <summary>
    /// Gets the Message value when this instance represents that case.
    /// </summary>
    public Message? Message { get; }

    /// <summary>
    /// Gets the Boolean value when this instance represents that case.
    /// </summary>
    public bool? Boolean { get; }

    /// <summary>
    /// Creates a MessageBoolean value from <c>Message</c>.
    /// </summary>
    public static MessageBoolean From(Message value)
    {
        return new MessageBoolean(value);
    }

    public static implicit operator MessageBoolean(Message value)
    {
        return From(value);
    }

    public bool TryGetMessage(out Message? value)
    {
        value = Message;
        return value is not null;
    }

    /// <summary>
    /// Creates a MessageBoolean value from <c>bool</c>.
    /// </summary>
    public static MessageBoolean From(bool value)
    {
        return new MessageBoolean(value);
    }

    public static implicit operator MessageBoolean(bool value)
    {
        return From(value);
    }

    public bool TryGetBoolean(out bool? value)
    {
        value = Boolean;
        return value is not null;
    }

}

file sealed class MessageBooleanJsonConverter : JsonConverter<MessageBoolean>
{
    public override MessageBoolean Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        if (reader.TokenType is JsonTokenType.True or JsonTokenType.False)
        {
            return MessageBoolean.From(reader.GetBoolean());
        }

        if (reader.TokenType != JsonTokenType.StartObject)
        {
            throw new JsonException("Unable to deserialize MessageBoolean: unexpected JSON token.");
        }

        var objectReader = reader;
        ReadObjectMetadata(
            ref objectReader,
            out var hasChatProperty,
            out var hasDateProperty,
            out var hasMessageIdProperty);

        if (hasChatProperty && hasDateProperty && hasMessageIdProperty)
        {
            return MessageBoolean.From(JsonSerializer.Deserialize<Message>(ref reader, options)
                ?? throw new JsonException("Unable to deserialize MessageBoolean as Message."));
        }

        throw new JsonException("Unable to deserialize MessageBoolean from the provided Telegram payload.");
    }

    private static void ReadObjectMetadata(
        ref Utf8JsonReader reader,
        out bool hasChatProperty,
        out bool hasDateProperty,
        out bool hasMessageIdProperty)
    {
        hasChatProperty = false;
        hasDateProperty = false;
        hasMessageIdProperty = false;

        while (reader.Read())
        {
            if (reader.TokenType == JsonTokenType.EndObject)
            {
                return;
            }

            if (reader.TokenType != JsonTokenType.PropertyName)
            {
                throw new JsonException("Unable to scan union object metadata: expected a JSON property name.");
            }

            if (reader.ValueTextEquals("chat"u8))
            {
                hasChatProperty = true;
                if (!reader.Read())
                {
                    throw new JsonException("Unable to scan union object metadata: expected a JSON property value.");
                }

                reader.Skip();
                continue;
            }

            if (reader.ValueTextEquals("date"u8))
            {
                hasDateProperty = true;
                if (!reader.Read())
                {
                    throw new JsonException("Unable to scan union object metadata: expected a JSON property value.");
                }

                reader.Skip();
                continue;
            }

            if (reader.ValueTextEquals("message_id"u8))
            {
                hasMessageIdProperty = true;
                if (!reader.Read())
                {
                    throw new JsonException("Unable to scan union object metadata: expected a JSON property value.");
                }

                reader.Skip();
                continue;
            }

            if (!reader.Read())
            {
                throw new JsonException("Unable to scan union object metadata: expected a JSON property value.");
            }

            reader.Skip();
        }

        throw new JsonException("Unable to scan union object metadata: object was not closed.");
    }

    public override void Write(Utf8JsonWriter writer, MessageBoolean value, JsonSerializerOptions options)
    {
        ArgumentNullException.ThrowIfNull(value);
        if (value.Message is not null)
        {
            JsonSerializer.Serialize(writer, value.Message, options);
            return;
        }

        if (value.Boolean is not null)
        {
            JsonSerializer.Serialize(writer, value.Boolean, options);
            return;
        }

        throw new JsonException("The MessageBoolean instance does not contain a union case value.");
    }
}